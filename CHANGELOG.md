# Changelog (ACTA Contracts)

All relevant changes are documented here from the ACTA migration onward.

## 0.1.0
- Complete rebranding from `Chaincerts` to `ACTA`.
- Renamed packages and folders to `acta_*_contract`.
- Updated `build.sh` and `release.sh` scripts.
- Cleaned up READMEs and tests to use `did:acta:`.